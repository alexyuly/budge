extends layout

block content
  h2 Navigation
  ul
    li: a(href="../manage_users") Back to Manage Users...
  h2 New User
  form(action="new_user", method="post")
    label Name 
      input(type="text", name="name", autoFocus=true)
    button(type="submit") Submit
