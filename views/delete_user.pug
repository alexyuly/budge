extends layout

block content
  h2 Delete User "#{user.name}"?
  ul
    li: a(href=`../edit_user/${user._id}`) No, Cancel and Go Back...
  form(action=user._id, method="post")
    button(type="submit") Yes, Delete User "#{user.name}"
