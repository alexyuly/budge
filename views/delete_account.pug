extends layout

block content
  h2 Delete Account "#{account.name}"?
  ul
    a(href=`../edit_account/${account._id}`): li No, Cancel and Go Back...
  form(action=account._id, method="post")
    button(type="submit") Yes, Delete Account "#{account.name}"
